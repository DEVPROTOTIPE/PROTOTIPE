/**
 * Stub de compilación para la feature de Billing.
 * Si esta feature no se inyecta de forma real durante el aprovisionamiento,
 * este archivo provee las funciones necesarias para evitar fallos de resolución en Rollup/Vite.
 */

export function subscribeToBillingData(callback) {
  // Retorna una función de desuscripción vacía (no-op)
  return () => {}
}
