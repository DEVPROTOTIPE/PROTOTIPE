export const {{upperName}}_ACTIONS = {
  LOAD_START: '{{featureId}}/load-start',
  LOAD_SUCCESS: '{{featureId}}/load-success',
  LOAD_FAILURE: '{{featureId}}/load-failure'
};

export const COLLECTION_NAME = '{{featureId}}';
